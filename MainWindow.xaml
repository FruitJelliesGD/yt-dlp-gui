<Window x:Class="yt_dlp_gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="yt-dlp-gui"
        Width="900"
        Height="560"
        Background="#1E1E1E"
        Foreground="#E6E6E6">

    <!-- ================= 全局深色资源 ================= -->
    <Window.Resources>

        <!-- 文本 -->
        <SolidColorBrush x:Key="TextBrush" Color="#E6E6E6"/>
        <SolidColorBrush x:Key="SubTextBrush" Color="#B0B0B0"/>

        <!-- 输入框 -->
        <SolidColorBrush x:Key="InputBg" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="InputBorder" Color="#3C3C3C"/>

        <!-- 按钮 -->
        <SolidColorBrush x:Key="BtnBg" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="BtnHover" Color="#4A4A4A"/>
        <SolidColorBrush x:Key="BtnPressed" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="BtnBorder" Color="#555555"/>

        <!-- ListView -->
        <SolidColorBrush x:Key="ListBg" Color="#252526"/>
        <SolidColorBrush x:Key="ListBorder" Color="#3C3C3C"/>
        <SolidColorBrush x:Key="ListItemHover" Color="#333333"/>
        <SolidColorBrush x:Key="ListItemSelected" Color="#094771"/>

        <!-- 表头 -->
        <SolidColorBrush x:Key="HeaderBg" Color="#2D2D30"/>
        <SolidColorBrush x:Key="HeaderBorder" Color="#3F3F46"/>

        <!-- ================= 样式 ================= -->

        <!-- TextBlock -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,2"/>
        </Style>

        <!-- Button -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BtnBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BtnBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BtnHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource BtnPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GridView 表头 -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource HeaderBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource HeaderBorder}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>

        <!-- ListViewItem -->
        <Style TargetType="ListViewItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                        Background="{TemplateBinding Background}">
                            <GridViewRowPresenter
                        Columns="{Binding Path=View.Columns,
                                  RelativeSource={RelativeSource AncestorType=ListView}}"
                        Content="{TemplateBinding Content}"
                        Margin="2"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd"
                                Property="Background"
                                Value="{StaticResource ListItemHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd"
                                Property="Background"
                                Value="{StaticResource ListItemSelected}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


    </Window.Resources>

    <!-- ================= 主布局 ================= -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- URL -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="视频链接 (URL)" FontWeight="Bold"/>
            <TextBox x:Name="UrlTextBox" Height="30"/>
        </StackPanel>

        <!-- 保存路径 -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="保存路径" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Grid.Column="1"
                     x:Name="PathTextBox"
                     Height="30"
                     Margin="10,0"
                     IsReadOnly="True"/>
            <Button Grid.Column="2"
                    Content="选择文件夹"
                    Width="100"
                    Click="SelectPath_Click"/>
        </Grid>

        <!-- 格式 + Cookies -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Width="369">
                <TextBlock Text="格式 ID" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox x:Name="FormatIdTextBox"
                         Height="30"
                         Margin="10,0"
                         HorizontalAlignment="Stretch"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="Cookies" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox x:Name="CookiesPathTextBox"
                         Width="220"
                         Height="30"
                         Margin="10,0"
                         IsReadOnly="True"/>
                <Button Content="选择文件"
                        Width="90"
                        Click="SelectCookies_Click"/>
            </StackPanel>
        </Grid>

        <!-- 操作按钮 -->
        <Grid Grid.Row="3" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="查看可用格式 (-F)"
                    Height="36"
                    Margin="0,0,10,0"
                    Click="CheckFormats_Click"/>

            <Button Grid.Column="1"
                    Content="开始下载"
                    Height="36"
                    Click="DownloadVideo_Click"/>
        </Grid>

        <!-- 任务列表 -->
        <ListView Grid.Row="4"
          x:Name="TaskListView"
          Background="#252526"
          Foreground="#D4D4D4"
          BorderBrush="#3C3C3C"
          BorderThickness="1">

                <ListView.View>
                    <GridView>

                        <!-- URL -->
                        <GridViewColumn Header="URL" Width="380">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Url}"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="#D4D4D4"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- 状态 -->
                        <GridViewColumn Header="状态" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Status}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- 速度 -->
                        <GridViewColumn Header="速度" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Speed}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- 进度 -->
                        <GridViewColumn Header="进度" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Height="18" Width="100">
                                        <ProgressBar Minimum="0"
                                         Maximum="100"
                                         Value="{Binding Progress}"/>
                                        <TextBlock Text="{Binding Progress, StringFormat={}{0:F1}%}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="11"
                                       FontWeight="SemiBold" Foreground="Black"/>
                                    </Grid>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                    </GridView>
                </ListView.View>
            </ListView>


    </Grid>
</Window>
